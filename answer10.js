function Hero(name, level) {
    this.name = name;
    this.level = level;
}

a constructor function is initialized with anumber of parameters,which would be assigned as propertiesof 'this' , referring to the function itself
